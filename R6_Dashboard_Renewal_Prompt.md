# R6 Siege Stats Dashboard — リニューアル指示プロンプト（v2）

あなたはLinear、Vercel、Raycast、Arc Browser、Stripeのプロダクトデザインを担当してきたシニアプロダクトデザイナーです。Figmaでデザインシステムを組んでからコードに落とす、実務レベルの精度で作業してください。

Dribbbleのコンセプトアートではなく、**実際にプロダクションで動いているUI**を基準にしてください。装飾を「足す」のではなく、余白・タイポグラフィ・階層・モーション・質感の精度で高級感を出してください。

---

## 0. 変更してはいけないもの（最優先で保持）

- プレイヤーカードのホログラム風エフェクト・グラデーション・光沢表現
- カードのレイアウト、サイズ、情報配置
- ランクバッジ、Threat/MVPバッジのデザイン
- 現在の機能・データ構造・表示ロジック

→ **カードだけがゲームらしく発光する「主役」で、それ以外は静かな高級SaaSの器（かんき）である**、という現状のコンセプトは維持したまま、器側の作り込みの精度だけを引き上げます。

---

## 1. コンセプト

「Quiet Luxury Console」

ゲームサイトの語彙（ネオン、六角形、SF回路、ハニカム、ゲーミングRGB）を一切使わず、Linear/Vercel/Raycastのような開発者向けプロ製品の語彙で画面を構成し、その中にゲーミングカードだけが宝石のように置かれている状態を目指します。

参考にする実在のプロダクト（Dribbbleではなく実サービス）：
- **Linear** … ほぼ純黒（#0A0A0A系）の面、影ではなく明度差で階層をつくる、圧縮された見出しタイポ
- **Vercel** … モノクロのCTAボタン、影の代わりの単色シャドウ体系、グラデーションの実装品質
- **Raycast** … void-black(#040506)〜graphite-700(#111214)のグレースケール積層、単一アクセントカラーの徹底
- **Stripe** … グラデーション/シェーダー表現のゴールドスタンダード、データテーブルの見せ方
- **Arc Browser** … プロダクトUIそのものをヒーローに見せる構成、ライフスタイル写真を使わない潔さ

---

## 2. 最重要ルール：アクセントカラーは「1色のオーナーシップ」

現状はBlue/Purple/Cyan/Gold/Redの5色が並列に使われており、これが最も「AI生成っぽさ」を出している原因です。トップ層のプロダクト（Linear=Purple、Raycast=Red-orange、Cursor=Cyan）はブランドカラーを1色に絞り込み、他の色は完全に機能色として封印しています。

このルールに従ってください：

- **ブランドアクセント（サイト全体で使う唯一の装飾色）を1色だけ選ぶ**（例：エレクトリックブルーまたはインディゴパープルのどちらか一方。両方は使わない）
- Gold（MVP）、Red（Threat）、Green（Win率など）は**装飾ではなく意味を持つ機能色としてのみ**使用可（バッジ、ステータス表示に限定）
- Cyanは背景のグロー演出のみに残し、UIコンポーネント（ボタン、ボーダー、フォーカスリング）には使わない
- カード内部の既存カラーリングは変更しない（0章のルールが優先）

---

## 3. ベースカラー・階層設計

単色フラット背景は禁止。ただし「盛る」方向ではなく「絞る」方向で作ってください。

- ベースは黒に近いグレースケールを3〜4段階だけ用意し、影ではなく明度差で階層をつくる
  - 例：`#050608`（背景）→ `#0A0C10`（セクション）→ `#111318`（パネル）→ `#181B22`（ホバー時）
- 背景装飾は「大きく、遠く、薄く」：
  - 画面上部に1つ、下部に1つ程度の大型ぼかしグロー（アクセントカラー＋その補色に近いニュートラルトーンのみ、2色まで）
  - 微細なノイズテクスチャ（3〜4%程度のopacity）でプラスチック感を消す
  - メッシュグラデーションは装飾として主張させず、あくまで「奥にうっすら光がある」程度に留める
- 影は基本的に使わず、1px境界線（`rgba(255,255,255,0.06〜0.1)`）で面を区切る。使う場合も単色でぼかしを最小限に

---

## 4. レイアウト・情報設計

- 「詰め込まない」ではなく「見せる情報の優先順位を決める」こと。今の画面はチーム全体を常に同じ密度で見せているため、まず**MVP/Threatなど"今いちばん見るべき情報"を上位階層に**、個々の詳細は視線を落とした先に配置する進行的開示（progressive disclosure）の構造にする
- 左右の味方/敵チームは、1枚の大きなガラスパネルで囲うのではなく、**それぞれ独立したセクションとして明確な余白で分離**し、パネルの境界は太いガラス風より細いボーダー1本の方が高級感が出る
- グリッドは均等配置ではなく、bento風（サイズ差のある非対称グリッド）も検討可。ただしカードサイズ自体は変更不可なので、カード「群」の配置間隔・グルーピング単位で表現する
- ヘッダーはLinear/Raycastのように圧縮された太めの見出し＋控えめなサブテキストの2段構成。API入力欄は目立たせすぎず、Cmd+K的な「必要な時だけ主張する」トーンに（キラキラアイコンやAI感を煽る装飾は避ける）

---

## 5. コンポーネント（ボタン・入力欄）

- ボタン：極細ボーダー、低彩度の背景、ホバー時のみアクセントカラーで光る。塗りつぶしボタンは白または最も暗いニュートラルのみ（Vercelのモノクロームボタンを参考に）
- 入力欄（Gemini API欄など）：Raycast/Vercelのinputのように、フォーカス時にアクセントカラーのリング（2px以下、ぼかし控えめ）が出るだけのミニマル構成。プレースホルダーや装飾アイコンは最小限に

---

## 6. タイポグラフィ

- 見出し：太め・字間をわずかに詰める（圧縮タイポ）
- 本文/ラベル：細め、行間は広め
- 数値（K/D、WIN%など）：等幅または数字専用フォント特性を活かし、桁が揃って見えるように。ここはカード内なので変更不可だが、カード外の集計表示があれば同じ思想で統一

---

## 7. モーション

過剰な演出は禁止。以下の範囲のみ：
- 要素の出現時に短いフェード＋わずかな上方向スライド（scroll-linked fadeなど、Framer Motion相当のイージング）
- ホバー時のみ：軽いスケール（1.01〜1.02倍程度）とアクセントカラーの控えめなグロー
- クリック時：わずかな沈み込み（scale down）でフィードバックを出す
- ローディング状態（プレイヤー情報取得中など）はスケルトン表示で作り込む。ここを雑に扱うと一気に「AI生成感」が出るため要注意

---

## 8. 明示的にやらないこと（NG）

- ネオン全面、六角形、SF回路、ハニカム、過度なサイバー感、ゲーミングRGB
- 5色以上のアクセントカラーを並列使用すること
- 半透明パネルを多用する「安っぽいグラスモーフィズム」
- キラキラ・AIらしさを強調するアイコン/コピー
- 情報量を今より増やすこと
- Dribbbleにありがちな「装飾のための装飾」（無意味な光の粒子、過剰なブラー、意味のないグラデーション帯）

---

## 9. 優先順位

1. プレイヤーカードを画面内で最も目立たせる（変更しない）
2. アクセントカラーを1色に絞り、機能色と装飾色を分離する
3. 明度階層とボーダーで面を構成し、影・グローを最小限にする
4. 余白と進行的開示で情報密度をコントロールする
5. モーションとローディング状態まで含めて実装レベルの完成度に仕上げる

---

現在の機能・データ構造・レイアウトの骨格は維持したまま、UIの質感・カラー戦略・情報階層のみを上記方針で全面的にブラッシュアップしてください。
